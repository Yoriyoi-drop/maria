═══════════════════════════════════════════════════════════════════════════════
13. SRP REFACTORING PROGRESS (2026-07-26)
═══════════════════════════════════════════════════════════════════════════════
user = skip dulu 
Memisahkan file-file dengan >1 tanggung jawab menjadi 1 file = 1 tanggung jawab.

✅ SELESAI:

13.1 elaborator.rs → ast/types.rs
    impl DataType { fn width() } & impl DeclKind { fn default_width() } dipindah.

13.2 simulator/engine/mod.rs → engine_utils.rs
    3 helper functions (evaluate_string_method, sym_char_matches, edge_matches_abbrev)
    dipindah ke simulator/engine/engine_utils.rs.

13.3 elaboration/util.rs (1709 baris) → 9 submodule files:
    type_util.rs, param_util.rs, generate.rs, loop_unroll.rs,
    signal_analysis.rs, const_fold.rs, operator.rs, width.rs, type_subst.rs

✅ TOTAL: 3 fase refactoring — 9 file baru — 0 test rusak.

🔜 SELANJUTNYA:
    parser.rs (7714 baris), elaborator.rs (5571 baris), main.rs (927 baris),
    simulator/engine/eval.rs (3396 baris)

========================================

[COMPLETED] Phase 3.5: Ekstraksi elaborator/mod.rs (4452 -> 3 file)
├── elaborator/mod.rs       ~2053 lines (Core: struct, impl new, elaborate, resolve_param_values, elaborate_classes)
├── elaborator/stmt.rs      ~1150 lines (elaborate_stmt_block, elaborate_stmt, elaborate_lvalue)
├── elaborator/expr.rs      ~1250 lines (elaborate_expr, package func call, expr-to-signal, resolve_typedef_width)
├── Perbaikan:
│   ├── pub(crate) visibility: build_hier_name, elaborate_expr, elaborate_expr_to_signal,
│   │   instance_port_expr_to_signal, resolve_typedef_width, elaborate_stmt_block,
│   │   elaborate_stmt, elaborate_lvalue
│   ├── Fix duplicate closing brace di expr.rs (line 4452 sdh punya impl close)
│   └── Added use super::BUILTIN_UVM_CLASSES di expr.rs
├── Total submodules elaborator/: ext, flatten, always, stmt, expr (+ mod.rs)
└── Semua test lulus ✅ (5 passed)

[COMPLETED] Phase 4: Ekstraksi parser/mod.rs (4032 -> 1105 + 3 submodule)
├── parser/mod.rs          ~1105 lines (Core: struct, impl new, parse_design, parse_module_item dispatcher, skip utilities)
├── parser/decl.rs         ~1147 lines (parse_decl, parse_decl_names, parse_enum_members, parse_struct_body,
│                          parse_typedef, parse_scoped_type_name, parse_type_expr, parse_param_list, parse_range)
├── parser/instance.rs     ~853 lines (parse_module, parse_interface, parse_interface_fast, parse_program_fast,
│                          skip_balanced_paren, parse_modport, parse_port_list, parse_instance, parse_gate_primitive)
├── parser/proc.rs         ~968 lines (parse_always, parse_initial, parse_final, parse_sensitivity_events,
│                          parse_sensitivity_list, parse_assign, parse_delay,
│                          parse_function, parse_task,
│                          parse_generate_block, parse_generate_item, parse_generate_block_body)
├── Perbaikan:
│   ├── pub(crate) visibility di semua fungsi (30 total)
│   ├── use super::Parser; + crate imports di setiap submodule baru
│   └── use crate::parser::util::*; di instance.rs (is_strength_keyword)
├── Total submodules parser/: lexer, preprocessor, util, specify, udp, config, package,
│                          class, expr, stmt, decl, instance, proc (+ mod.rs)
└── Semua test lulus ✅ (5 passed)

[COMPLETED] Phase 5: Ekstraksi ast/inline.rs (2599 -> 1858 + inline_util.rs 747)
├── ast/inline.rs           ~1858 lines (Core: inline_func_calls_in_module, inline_funcs_in_stmt, replace_func_calls_in_expr)
├── ast/inline_util.rs      ~747 lines (Helpers: detect_recursive_functions, stmt_has_func_call, expr_has_func_call,
│                          func_port_width, func_return_width, rename_in_stmt, rename_func_decls_in_stmt, rename_in_expr)
├── Perbaikan:
│   ├── pub(crate) visibility di semua 8 helper functions
│   ├── Added use crate::ast::inline_util::*; di inline.rs
│   ├── Added Decl import di inline_util.rs
│   └── Updated ast/mod.rs: pub mod inline_util; + pub use inline_util::*;
└── Semua test lulus ✅ (5 passed)

[COMPLETED] Phase 6: Ekstraksi simulator/packed.rs (1374 -> 587 + packed_eval.rs 787)
├── simulator/packed.rs       ~587 lines (PackedLogicVec struct + impl blocks + Display impl)
├── simulator/packed_eval.rs  ~787 lines (eval_unary_packed, eval_binary_packed, eval_binary_packed_extended)
├── Perbaikan:
│   ├── Hapus dangling doc comment di akhir packed.rs
│   ├── Update import di eval/expr.rs: packed → packed_eval untuk eval functions
│   └── Added pub mod packed_eval; di simulator/mod.rs
└── Semua test lulus ✅ (5 passed)

[COMPLETED] Phase 7a: Ekstraksi SysCall handler dari scheduler/block.rs (3060 -> 1920 + block_syscall.rs 1342)
├── scheduler/block.rs          ~1920 lines (Core: block evaluation, control flow, loops, fork, assertions, method calls)
├── scheduler/block_syscall.rs  ~1342 lines ($display, $fopen, $readmemh, $urandom, $asserton, $coverage_*, dll.)
├── scheduler/mod.rs            +pub mod block_syscall
├── Perbaikan:
│   ├── Hapus unused imports (waveform, ast, parallel, VecDeque, io) dari block.rs
│   ├── Tambah kembali import yg diperlukan: rand::Rng, crate::Symbol
│   ├── Fix name.as_str() — SysCall pattern match menghasilkan Symbol, bukan &str
│   ├── Bersihkan dead code coverage_merge di block_syscall.rs
│   └── Deduplikasi evaluate_syscall_stmt → delegasi ke evaluate_syscall
└── Semua test lulus ✅ (919 passed, 0 failed)

# Maria HDL (.mv) — Spesifikasi Bahasa

> **Status:** Draf Desain v0.1 — fondasi implementasi `maria mgen`
> **Versi target Maria:** 0.4.0
> **Ekstensi sumber:** `.mv`
> **Ekstensi output:** `.sv` (modul/program/class) + `.svh` (package/typedef/interface)

---

## 1. Pendahuluan

**Maria HDL** (ekstensi `.mv`) adalah bahasa deskripsi perangkat keras milik Maria —
bahasa **baru total** dengan sintaksnya sendiri, yang **tidak** ditulis langsung
sebagai SystemVerilog. Sumber kebenaran (source of truth) ada di file `.mv`;
SystemVerilog hanyalah **output** yang di-generate otomatis oleh tool `maria mgen`.

Tujuannya, mengikuti pendekatan Chisel/SpinalHDL terhadap Verilog:

> **Tulis desain sekali dengan bersih → generate SystemVerilog yang valid, indah,
> dan terbaca manusia → simulasi dengan Maria (atau Verilator/VCS/Vivado).**

`maria mgen` melakukan **source-to-source transpilation**: `.mv` di-lex, di-parse
ke AST `.mv`, di-*type-check*, lalu di-emit menjadi `.sv` + `.svh`.

```
counter.mv ──→ [lexer .mv] ──→ [parser .mv] ──→ [AST .mv] ──→ [type-check]
                                                            │
                                                            ▼
                                       [codegen] ──→ counter.svh (package/typedef)
                                          │       └─→ counter.sv  (module)
                                          ▼
                              pipeline Maria NORMAL (parser SV → elaborasi → sim)
                              atau tool EDA lain (Verilator, VCS, Vivado)
```

---

## 2. Prinsip Desain

| # | Prinsip | Arti |
|---|---------|------|
| 1 | **SV adalah output, bukan sumber** | Tidak ada cara "menulis SV" di dalam `.mv`. Setiap konstruk `.mv` punya mapping SV yang pasti & deterministik. |
| 2 | **Deterministik** | `.mv` yang sama selalu menghasilkan `.sv`/`.svh` yang byte-identik. Hasil generate boleh di-commit ke repo. |
| 3 | **Zero boilerplate** | Port list, `always_ff` + reset, `generate`/`genvar`, dan `typedef` ditangani bahasa — bukan ditulis tangan. |
| 4 | **Type-check sebelum generate** | Error lebar-bit, arah port, dan sinyal tak dikenal muncul di level `.mv` (diagnostics Maria), bukan di SV hasil generate. |
| 5 | **Output indah & terbaca manusia** | Emitter menghasilkan SV terindentasi rapi, diberi komentar, siap dibaca/direview manusia (pola Chisel). |
| 6 | **Standar SV 100% valid** | Output bisa masuk tool apa pun (Verilator, VCS, Vivado, Yosys) tanpa modifikasi. |
| 7 | **Satu file .mv → satu .sv + satu .svh** | `.svh` berisi definisi bersama (package/typedef/interface); `.sv` berisi modul/program/class. |
| 8 | **Inkremental & ter-cache** | Hasil generate di-cache MICD; hanya `.mv` yang berubah yang di-reprocess. |

---

## 3. Konvensi Umum

### 3.1 Komentar

```mv
// komentar satu baris
/* komentar
   multi baris */
```

### 3.2 Identifiers

- `[a-zA-Z_][a-zA-Z0-9_]*` — sama seperti SV (agar 1:1 ke output).
- Kata kunci bahasa `.mv` **huruf kecil** dan reserved (tidak bisa jadi nama sinyal).
- Konvensi (tidak dipaksakan compiler, dipakai codegen untuk dokumentasi):
  - sinyal/port: `snake_case`
  - tipe & class: `PascalCase`
  - konstanta/parameter: `UPPER_SNAKE`
  - sinyal reset aktif-low: akhiran `_n`

### 3.3 Struktur file

```
types.mv        → types.svh      (package + typedef + interface, di-`include`)
counter.mv      → counter.sv     (module) + counter.svh (jika punya tipe sendiri)
tb_counter.mv   → tb_counter.sv  (program/testbench — TANPA .svh)
```

Aturan nama output:
- `foo.mv` → `foo.sv` + `foo.svh` (di direktori yang sama, atau `-o <dir>`).
- `.svh` HANYA digenerate jika file punya definisi bersama (package atau
  typedef level file). Tanpa keduanya, `.sv` berdiri sendiri — tidak ada file
  `.svh` dan tidak ada baris `` `include `` di `.sv`. (Sesuai emisi `mgen`).
- Jika file `.mv` tidak memiliki definisi bersama (package/typedef), file
  `.svh` TIDAK digenerate sama sekali — tidak ada `include guard` kosong.

### 3.4 Header hasil generate

Setiap file output diawali:

```systemverilog
// ─────────────────────────────────────────────────────────────
// Generated by maria mgen v0.4.0 — DO NOT EDIT
// Sumber: counter.mv
// Regenerate: maria mgen counter.mv
// ─────────────────────────────────────────────────────────────
```

`DO NOT EDIT` bersifat informatif; karena hasil di-commit, regenerasi aman
karena deterministik.

---

## 4. Tipe Data

### 4.1 Tipe dasar

| `.mv` | Arti | Emisi SV |
|-------|------|----------|
| `bit` | 1-bit, 2-state | `bit` |
| `logic` | 1-bit, 4-state | `logic` |
| `logic[N]` | vektor N-bit `[N-1:0]` | `logic [N-1:0]` |
| `logic[MSB:LSB]` | vektor range eksplisit | `logic [MSB:LSB]` |
| `signed logic[N]` | vektor signed | `logic signed [N-1:0]` |
| `int` | 32-bit signed | `int` |
| `uint` | 32-bit unsigned | `logic [31:0]` |
| `int[N]` / `uint[N]` | vektor 32-bit × N | `int [N]` / `logic [31:0] [N]` |
| `longint` / `ulongint` | 64-bit | `longint` / `longint unsigned` |
| `shortint` | 16-bit | `shortint` |
| `byte` | 8-bit signed | `byte` |
| `real` | f64 | `real` |
| `time` | 64-bit time | `time` |
| `string` | string | `string` |

Aturan:
- `logic[N]` adalah **unsigned**; gunakan `signed logic[N]` bila perlu.
- `N` bisa konstanta/parameter/ekspresi const-foldable: `logic[WIDTH-1:0]`.

### 4.2 Array (unpacked)

```mv
sig : logic[8][4]        // 4 elemen, tiap 8-bit → logic [7:0] sig [0:3]
mat : uint[8][8]         // matriks 8×8
```

`Type[N][M]` = array N×M (indeks 0..N-1, 0..M-1). Emisi: `[0:N-1][0:M-1]`.

### 4.3 Alias tipe

```mv
type Addr = logic[15:0]
type Data = logic[31:0]
type ByteArray = logic[8][256]
```

Emisi (di package atau `$unit` bila dalam module):

```systemverilog
typedef logic [15:0] Addr;
typedef logic [31:0] Data;
typedef logic [7:0]  ByteArray [0:255];
```

### 4.4 Struct

```mv
packed struct Packet {
    valid : bit,
    addr  : Addr,
    data  : Data
}
```

Tanpa kata `packed` = unpacked. Emisi:

```systemverilog
typedef struct packed {
    bit  valid;
    Addr addr;
    Data data;
} Packet;
```

### 4.5 Enum

```mv
enum State { IDLE, RUN, DONE }
enum(3) Color { RED = 0, GREEN = 2, BLUE = 4 }   // lebar eksplisit 3-bit
```

Emisi:

```systemverilog
typedef enum logic [1:0] { IDLE, RUN, DONE } State;
typedef enum logic [2:0] { RED = 3'd0, GREEN = 3'd2, BLUE = 3'd4 } Color;
```

Lebar implisit = `clog2(jumlah anggota)` (minimal 1). Nilai boleh eksplisit
(`= expr`), nilai berikutnya auto-increment.

---

## 5. Package

```mv
package counter_pkg {
    type Addr = logic[15:0]
    packed struct Packet { ... }
    enum State { ... }
    const VERSION = 32'h20260808
}
```

Emisi:

```systemverilog
package counter_pkg;
    typedef logic [15:0] Addr;
    typedef struct packed { ... } Packet;
    typedef enum logic [1:0] { ... } State;
    localparam logic [31:0] VERSION = 32'h20260808;
endpackage
```

Penggunaan dari module `.mv`:

```mv
module counter {
    use counter_pkg::*
    ...
}
```

Emisi: `import counter_pkg::*;` di dalam module.

---

## 6. Module

### 6.1 Kerangka umum

```mv
module name #(params) {
    // ── ports ──
    in  ... 
    out ...

    // ── internals ──
    sig / reg / const

    // ── logika ──
    seq / comb / always / latch / initial / final

    // ── struktur ──
    inst / for / if
}
```

### 6.2 Parameter (generics)

```mv
module fifo #(
    WIDTH : int  = 8,          // param bertipe
    DEPTH : int  = 16,
    T     : type = logic[8]    // parameter tipe
) {
    ...
}
```

Emisi:

```systemverilog
module fifo #(
    parameter int      WIDTH = 8,
    parameter int      DEPTH = 16,
    parameter type     T     = logic [7:0]
) (...);
```

`type` parameter memungkinkan modul generik terhadap tipe sinyal.

### 6.3 Port

```mv
module counter {
    in  clk, rst_n : bit
    in  enable     : bit
    out count      : logic[7:0]
    out packet     : Packet
    inout data     : logic[7:0]
}
```

Emisi (ANSI port list):

```systemverilog
module counter (
    input  bit               clk,
    input  bit               rst_n,
    input  bit               enable,
    output logic [7:0]       count,
    output counter_pkg::Packet packet,
    inout  logic [7:0]       data
);
```

Aturan:
- Satu baris = satu arah; boleh banyak nama dipisah koma.
- Port `out` yang di-drive blok `seq`/`comb` **langsung** — tidak perlu internal
  reg + `assign` (valid SV: `output logic` boleh di-assign dari always block).
- Opsional: `out count : logic[7:0] = '0` → inisialisasi/reset value.

### 6.4 Signal, Register, Konstanta

```mv
// signal biasa (wire/var)
sig        : logic[7:0]
addr, data : logic[15:0]

// register (dengan reset value opsional)
reg count  : logic[7:0] = '0

// konstanta → localparam
const DEPTH_LOG = 4
const MASK      : logic[7:0] = 8'hF0
```

Emisi:

```systemverilog
logic [7:0] sig;
logic [15:0] addr, data;
logic [7:0] count;             // reset value dipakai di always_ff
localparam DEPTH_LOG = 4;
localparam logic [7:0] MASK = 8'hF0;
```

### 6.5 Blok logika: `seq`, `comb`, `always`, `latch`

#### `seq` — sequential (pengganti `always_ff`)

```mv
seq(clk, rst_n) {
    if (!rst_n) {
        count <= '0
    } else if (enable) {
        count <= (count == 99) ? '0 : count + 1
    }
}
```

Aturan reset:
- `seq(clk)` — tanpa reset.
- `seq(clk, rst_n)` — **async reset**; aktif-low otomatis bila nama berakhiran
  `_n`, aktif-high jika tidak.
- `seq(clk, rst_n, sync)` — reset **synchronous** (tanpa `or negedge`).
- `seq(negedge clk, ...)` — edge negatif.

Emisi:

```systemverilog
always_ff @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        count <= '0;
    end else if (enable) begin
        count <= (count == 99) ? '0 : count + 1;
    end
end
```

- Assignment **hanya** non-blocking `<=` di dalam `seq` (dipaksa type-check).
- Reset value register (`reg x : T = init`) otomatis menjadi isi cabang reset.

#### `comb` — combinational (pengganti `always_comb`)

```mv
comb {
    packet.valid = enable
    packet.addr  = count
    packet.data  = '0
}
```

Emisi:

```systemverilog
always_comb begin
    packet.valid = enable;
    packet.addr  = count;
    packet.data  = '0;
end
```

- Assignment **hanya** blocking `=` di dalam `comb` (dipaksa type-check).
- Sensitivity list di-infer otomatis (sama seperti `always_comb`).

#### `always` / `latch`

```mv
always {
    @(posedge clk)
    x = y + 1
}
latch {
    if (en) q = d
}
```

Emisi: `always begin ... end` / `always_latch begin ... end`.

### 6.6 Statement dalam blok

| `.mv` | Emisi SV |
|-------|----------|
| `if (c) { ... } else { ... }` | `if (c) begin ... end else begin ... end` |
| `case (e) { 0: { ... } 1, 2: { ... } default: { ... } }` | `case (e) 0: begin ... end 1, 2: ... default: ... endcase` |
| `for i in 0..N { ... }` (behavioral) | `for (int i = 0; i < N; i = i + 1) begin ... end` |
| `while (c) { ... }` | `while (c) begin ... end` |
| `repeat (n) { ... }` | `repeat (n) begin ... end` |
| `forever { ... }` | `forever begin ... end` |
| `wait (c) { ... }` | `wait (c) begin ... end` |
| `@(posedge clk) stmt` | `@(posedge clk) stmt` |
| `#10 stmt` | `#10 stmt` |
| `sig = expr` | `sig = expr;` (blocking) |
| `sig <= expr` | `sig <= expr;` (non-blocking) |
| `return expr` | `return expr;` |
| `break` / `continue` | `break;` / `continue;` |

Operand/operator: sama semantiknya dengan SV (`+ - * / % ** << >> & | ^ ~ ! && || == != === !== < <= > >= ? :`, concat `{a,b}`, replication `{n{a}}`, literal `'0 '1 'x 'z`, `8'hFF`, `32'd10`, `1.5`).

### 6.7 Instansiasi

```mv
// named — .port(signal); .port saja = auto-connect ke sinyal bernama sama
inst counter_small u_small (.clk, .rst_n, .enable, .count(count[3:0]))

// positional
inst alu u_alu (a, b, op, y)

// arrayed
inst flipflop u_ff[8] (.clk, .d(d_in), .q(q_out))
```

Emisi:

```systemverilog
counter_small u_small (
    .clk   (clk),
    .rst_n (rst_n),
    .enable(enable),
    .count (count[3:0])
);
alu u_alu (a, b, op, y);
flipflop u_ff[8] (
    .clk (clk),
    .d   (d_in),
    .q   (q_out)
);
```

Parameter instance: `inst fifo #(.DEPTH(32)) u_fifo (...)`.

### 6.8 Generate (`for` / `if` di level module)

`for`/`if` di **badan module** (di luar blok logika) adalah generate:

```mv
module shiftreg #(N : int = 8) {
    in  clk : bit
    in  d   : bit
    out q   : logic[N-1:0]

    seq(clk) {
        q[0] <= d
    }
    for i in 1..N {
        seq(clk) {
            q[i] <= q[i-1]
        }
    }
}
```

Emisi (label `gen_<loopvar>` otomatis):

```systemverilog
always_ff @(posedge clk) begin
    q[0] <= d;
end
generate
    for (genvar i = 1; i < N; i = i + 1) begin : gen_i
        always_ff @(posedge clk) begin
            q[i] <= q[i-1];
        end
    end
endgenerate
```

### 6.9 Function & Task

```mv
func clog2(x : int) -> int {          // function → otomatis jadi fungsi SV
    var r : int = 0
    var n : int = x - 1
    while (n > 0) {
        r = r + 1
        n = n >> 1
    }
    return r
}

task send(data : logic[7:0], out ok : bit) {
    #10
    ok = 1
}
```

Emisi: `function int clog2(int x); ... endfunction` dan `task send(...); ... endtask`.
`var` di dalam function = variabel lokal (`automatic`).

Catatan F34: initializer variabel lokal (`var n : int = x - 1`) DAN `return expr`
bekerja penuh end-to-end — inline meng-emit init ke temp signal lalu `return`
diubah jadi assignment ke result slot (contoh: `examples/mv/func_local.mv` →
`clog2(256) = 8`). Gaya port non-ANSI (`input int x;` di baris terpisah)
juga didukung penuh.

### 6.10 Interface

```mv
interface axi_lite {
    in  clk : bit
    sig awaddr : logic[31:0]
    sig awvalid : bit
    sig awready : bit
    // ...

    modport slave {
        in  awaddr, awvalid
        out awready
    }
}
```

Emisi: `interface axi_lite; logic [31:0] awaddr; ... modport slave (...); endinterface`.
Interface di-emit ke `.svh`.

---

## 7. Testbench (initial / final / assert)

### 7.1 Stimulus

```mv
module tb_counter {
    in clk : bit       // di-connect dari testbench clock
    ...
    initial {
        rst_n = 0
        #100
        rst_n = 1
        enable = 1
        repeat (300) @(posedge clk)
        $finish
    }
    initial {
        clk = 0
        forever #5 clk = ~clk
    }
}
```

### 7.2 Assertion

```mv
// immediate
assert (count <= 99) $info("count ok") else $error("count overflow")

// concurrent
assert property (@(posedge clk) enable |-> count == $past(count) + 1)
```

Emisi 1:1 ke SV (`assert`, `assert property`). `$info`/`$error`/`$warning`
dipertahankan.

**Implementasi (F6):**
- `assert (cond) pass else fail` di-emit satu baris bila pass/fail statement
  sederhana (`assert (c) $info(...) else $fatal(...);`); blok multi-baris
  tetap di-emit `begin...end`.
- `assert property (...)` — body dipertahankan **raw** (teks persis di antara
  `(` dan `)`, termasuk parens) karena berisi operator SVA (`|->`, `##`, `[*]`)
  yang bukan token `.mv`. Emisi: `assert property {raw};`.
- `$finish`/`$display`/`$info`/`$error`/`$fatal`/`$past` di-lex sebagai
  identifier `$nama` → statement/ekspresi biasa (`$finish;`, `$display(...);`).
- **Severity tasks (F14 ✅)**: `$info`/`$warning`/`$error`/`$fatal`
  dieksekusi simulator dengan format `$display`-style: `$info`/`$warning`/
  `$error` mencetak (`Info:`/`Warning:`/`Error:`) dan simulasi lanjut;
  `$fatal` mencetak `Fatal:` lalu menghentikan simulasi SEKETIKA
  (statement setelahnya tidak dieksekusi; `final` block tetap berjalan).
- **Ringkasan & exit code (F15 ✅)**: tiap panggilan severity dihitung;
  di akhir sim `── Severity Summary ──` dicetak (hanya bila ada
  warning/error/fatal). `$fatal` membuat CLI `sim`/`run` keluar dengan
  status non-zero (setara assertion failed); `$error`/`$warning` hanya
  menambah counter dan tidak mengubah exit code.
- **UVM severity (F16 ✅)**: macro `uvm_info`/`uvm_warning`/`uvm_error`/
  `uvm_fatal` di `uvm_macros.svh` kini meng-emit `$info`/`$warning`/
  `$error`/`$fatal` (bukan `$display`+`$finish`) → format UVM standar
  (`Info: UVM_INFO <id>: <msg>`), masuk Severity Summary, `uvm_fatal`
  menghentikan sim seketika + exit code non-zero. Jalur method
  `uvm_report_info/warning/error/fatal(...)` (builtin `uvm_report_object`)
  juga disambungkan ke severity system via `emit_severity` — termasuk saat
  dipanggil TANPA `this.` prefix di body method class.
- **Fix preprocessor (F16 ✅)**: baris yang diawali backtick kini
  diperiksa — bila nama setelah backtick adalah macro yang SUDAH
  didefinisikan, itu invokasi (`` `uvm_info(...) ``) dan di-expand;
  sebelumnya semua baris backtick dianggap directive dan invokasi macro
  di awal baris di-skip diam-diam (macro UVM tidak pernah jalan).
- `repeat (n) @(posedge clk)` dan `forever #5 clk = ~clk` di-emit satu baris
  bila body-nya statement tunggal.

### 7.3 Program block

```mv
program test_runner {
    initial {
        run_test("my_test")
    }
}
```

Emisi: `program test_runner; ... endprogram`.

---

## 8. Class & UVM

```mv
class my_test extends uvm_test {
    field count     : uint[32]
    rand field seed : uint[8]
    constraint c { seed > 10, seed < 200 }

    func new(name : string) {
        super.new(name)
    }

    func build_phase() {
        uvm_config_db::set(this, "*.agent", "count", count)
    }

    task run_phase() {
        var seqr : uvm_sequencer
        seqr.start_item(item)
        seqr.finish_item(item)
        #100
    }
}
```

Aturan:
- `field` = variabel member; `rand field` = field `rand`.
- `func` = method tanpa delay; `task` = method boleh ber-delay.
- `constraint name { ... }` 1:1 ke SV.
- Emisi: class SV standar di file `.sv`.

**Implementasi (F7):**
- `func new(...)` (constructor) di-emit tanpa tipe return; `func f() -> T`
  → `function T f(...)`, tanpa `->` → `function void f(...)`.
- Method call `obj.method(args)` (termasuk `this`/`super`) didukung;
  static scoped `pkg::func(...)` → `pkg::func(...)`.
- Delay-only `#100` (tanpa statement lanjutan) → `#100;`.
- Type-check class: field/constraint/method divalidasi (E2001–E2007);
  `this`/`super`/nama method tidak divalidasi (konservatif).
- Tipe eksternal seperti `uvm_sequencer` (kelas UVM) tidak dikenal checker —
  contoh UVM di-generate dengan escape hatch `mgen --no-check` (lihat
  `examples/mv_uvm/my_test.mv`).
- **Subset constraint (F7)**: awalnya hanya ekspresi relasional/equality yang
  dipisah koma (`seed > 10, seed < 200`).
- **Constraint lanjutan (F12 ✅)**: `inside` (nilai & range `[lo:hi]`),
  `dist` (`:=` bobot-item, `:/` bobot-range), `if/else` kondisional, dan
  `solve a before b` — di-emit 1:1 ke SV dan dievaluasi penuh oleh solver
  constraint Maria (domain analysis + rejection):
  ```mv
  constraint c_adv {
      addr inside {[1:10], 20, 30},
      data dist { 0 := 1, [1:5] :/ 9 },
      if (mode == 1) { addr > 5 } else { addr < 100 },
      solve addr before data
  }
  ```
  Emisi: `if (mode == 1) { ... } else { ... }` (constraint block SV memakai
  `{ }`, BUKAN begin/end). Implementasi: `crates/maria-mv/src/{lexer,ast,parser,codegen,
  check}.rs` (sisi `.mv`) + `crates/maria-simulator/src/simulator/engine/uvm/constraint_solver.rs`
  (`eval_constraint_body` rekursif, domain analysis inside/dinst range) +
  `crates/maria-parser/src/expr.rs` (fix `:=` = `AssignOp`) + `crates/maria-simulator/src/simulator/engine/eval/
  {ast,expr}.rs` (evaluasi `inside`/`dist` normalisasi lebar & range).
  Domain analysis: nilai/range `inside` DAN `dist` diekstrak ke
  `VarDomain.inside` → guided generation (bukan rejection murni — penting
  untuk field lebar, mis. 32-bit). Range `[a:b]` di-parse SV sebagai
  `RangeSelect` base 0; guard base memastikan `inside {x[7:0]}` (member
  select asli) dievaluasi sebagai nilai tunggal, bukan rentang.
- **Fix precedence ternary (F13 ✅)**: `? :` di SystemVerilog precedence-nya
  PALING RENDAH (di bawah `||`). Sebelumnya `Token::Question` di parser SV
  tidak mengecek `min_prec`, sehingga RHS operator binary menelan ternary
  sebagai ekspresi lokal: `m == 3 ? 1 : 0` ter-parse `m == (3 ? 1 : 0)`
  (= `m == 1`) — assertion `assert (it.mode == 1 ? it.addr > 5 : 1)` di
  `.mv` salah gagal, dan const-fold `3 == 3 ? 1 : 0` menghasilkan 0 (bukan
  1). Fix: guard `0 < min_prec` pada `Token::Question` di
  `src/parser/expr.rs` — ternary hanya dibentuk di level ekspresi penuh
  (`min_prec == 0`), di RHS binary ia di-`break` dan ditangani loop pemanggil
  (LRM 1800 §11.4.11). Kini ternary di assertion/assignment/argumen/nested
  berfungsi benar di SV dan `.mv`.
- **Sequence UVM (F17 ✅)**: macro `uvm_do`/`uvm_do_with`/`uvm_rand_send`
  dan objection (`uvm_raise_objection`/`uvm_drop_objection`) kini berfungsi
  end-to-end di task `body()` class `uvm_sequence`:
  - `randomize()` pada class UVM (subclass `uvm_sequence_item` dkk) memakai
    solver constraint Maria (check builtin `randomize()` didahulukan dari
    dispatch hierarki UVM — sebelumnya `randomize not implemented`).
  - `randomize() with {field_constraint}` didukung di body method class:
    inline constraint AST dievaluasi solver dengan `current_this` sehingga
    field class (`addr`) bisa diakses langsung.
  - `it = new()` di method class mengisi `class_name` dari tipe deklarasi
    local (`my_item it;` di `decls` task — fix `parse_task`); tanpanya objek
    jadi "unknown class" (randomize/get_type_name gagal).
  - `$sformatf` didukung di jalur AST (body method class).
  - `!x.randomize() with {...}` (pola UVM) ter-parse benar: `with` melekat ke
    MethodCall dalam, `!` tetap di luar.
  - `uvm_drop_objection` → count 0 → `running=false` (end-of-test).

- **Phases & `run_test()` (F18 ✅)**: UVM berjalan otomatis dari
  `module tb; initial begin run_test("my_test"); end endmodule` — tanpa
  boilerplate:
  - `run_test("name")` membuat objek test, memanggil `new("uvm_test_top",
    null)`, lalu fase: `build_phase` → `connect_phase` →
    `end_of_elaboration_phase` → `start_of_simulation_phase` (sinkron
    root→children), `run_phase` (task — bisa suspend di delay, kontinuasi
    dijadwalkan event loop), dan saat semua objection turun:
    `extract_phase` → `check_phase` → `report_phase` → `final_phase`
    SEBELUM sim berhenti.
  - Child component boleh punya phase walau root tidak (mis. env punya
    `connect_phase`, test tidak) — propagasi ke children SELALU dijalankan.
  - `uvm_test_top` adalah handle global ke root test UVM — TB bisa baca
    field hasil (`uvm_test_top.env.drv.got`).
  - `get_next_item(it)` di driver `forever begin ... end` MEMBLOKIR saat
    queue sequencer kosong (suspend + retry 1 ns, bukan spin/return item
    null) — pola driver UVM berjalan benar tanpa busy-loop.
  - Komponen penuh: `uvm_env`/`uvm_agent` sekarang terdaftar sebagai
    built-in class (sebelumnya hanya test/driver/monitor/scoreboard/sequencer).

---

## 9. Semantik & Type-Check (dilakukan SEBELUM emisi)

| Check | Contoh error |
|-------|--------------|
| Sinyal tak dikenal | `E2001: undefined signal 'foo'` |
| Lebar bit tidak cocok (assignment) | `E2002: width 8 to 16-bit signal 'count'` |
| Arah port tidak valid | `E2003: cannot drive input port 'clk'` |
| `<=` di luar `seq` / `=` di dalam `seq` | `E2004: non-blocking assign only in seq` |
| Tipe tak dikenal | `E2005: unknown type 'Foo'` |
| Konstanta overflow | `E2006: literal 256 exceeds 8-bit` |
| Duplikasi nama | `E2007: signal 'x' already declared` |

**Semantik implementasi (`crates/maria-mv/src/check.rs`, Fase 4):**
- **E2002** di-trigger hanya saat RHS **lebih lebar** dari LHS (risiko truncation).
  Widening (RHS lebih sempit) diizinkan — zero/sign extension aman.
- **E2003** hanya ditegakkan di module **DUT** (tanpa blok `initial`/`final`).
  Module testbench boleh me-drive port `in`.
- **E2001/E2005 konservatif**: package/tipe/function yang tidak didefinisikan di
  file yang sama dianggap **eksternal** (file `.mv` lain) → dilewati, bukan error.
  Nama pemanggilan `f(...)` tidak divalidasi; hanya argumennya.
- `port` + `reg`/`sig` dengan nama sama diizinkan (reg implementasi port output).
- **Posisi `line:col` (F11)**: SEMUA error type-check kini berposisi — AST
  membawa span dari parser, dan `mgen`/`run` menampilkan snippet source +
  caret menunjuk kolom persis (gaya rustc):
  ```
  error[E1005]: counter.mv: 4:16: [E2001] undefined signal 'foo' — di 'm'
    --> counter.mv:4:16
     |
   4 |     comb { y = foo }
     |                ^
  ```
  Implementasi: `crates/maria-mv/src/{ast,parser,check,lib}.rs` (span), `mgen` +
  `run` via `mv::format_error()` (snippet + caret).
- Escape hatch: `mgen --no-check` untuk konstruk eksternal yang belum dipahami
  checker.
- **Mode batch (F9)**: `mgen a.mv b.mv` / `maria run a.mv b.mv` memakai
  `check_many` — konteks GABUNGAN (tipe/package/konstanta dari semua file),
  sehingga `use pkg::*` antar-file lolos. Duplikat package/tipe-level-file/
  class LINTAS-file → E2007 (error di level `.mv`, bukan SV hasil generate).

Diagnostics memakai infra `src/diagnostics/` Maria (kode error, span, warna).

---

## 10. Emisi Codegen — Aturan Mapping SV

Ringkasan mapping konstruk `.mv` → SV:

| `.mv` | SV |
|-------|----|
| `type X = T` | `typedef T X;` (di `.svh`/package atau `$unit`) |
| `packed struct S { ... }` | `typedef struct packed { ... } S;` |
| `enum E { ... }` | `typedef enum logic [..] { ... } E;` |
| `package p { ... }` | `package p; ... endpackage` (`.svh`) |
| `module m #(p) { ports }` | `module m #(...) (ports);` (`.sv`) |
| `in/out/inout` | `input/output/inout` + tipe |
| `sig : T` | `T sig;` |
| `reg r : T = v` | `T r;` + reset branch di `always_ff` |
| `const C = e` | `localparam C = e;` |
| `seq(clk[,rst][,sync])` | `always_ff @(posedge clk [or negedge rst_n])` |
| `comb { }` | `always_comb begin ... end` |
| `latch { }` | `always_latch begin ... end` |
| `always { }` | `always begin ... end` |
| `initial { }` / `final { }` | `initial begin ... end` / `final begin ... end` |
| `inst m u (...)` | `m u (...);` |
| `for i in A..B` (module body) | `generate for (genvar i = A; i < B; i = i + 1) begin : gen_i` |
| `use pkg::*` | `import pkg::*;` |
| `interface i { }` | `interface i; ... endinterface` (`.svh`) |
| `class c ...` | `class c ... endclass` (`.sv`) |
| `assert` / `assert property` | 1:1 |

### 10.1 Isi `.svh` vs `.sv`

- **`.svh`**: header + `include guard`, `package`, `typedef` (alias/struct/enum),
  `interface`, konstanta level file. HANYA digenerate bila file punya definisi
  bersama (package/typedef); file tanpa keduanya tidak menghasilkan `.svh`
  dan `.sv`-nya tidak meng-`include` apa pun.
- **`.sv`**: `module`, `program`, `class`, function/task level file.
  Jika `.mv` menggunakan tipe dari package, `.sv` diawali `` `include "xxx.svh" ``.

### 10.2 Penamaan

- Nama identifier dipertahankan 1:1 (`.mv` → SV identik).
- Sinyal internal hasil *desugaring* (mis. label generate) memakai awalan
  `gen_`/`__mv_` yang tidak bentrok dengan nama user.
- Tidak ada penyingkatan; output mengutamakan keterbacaan.

---

## 11. CLI: `maria mgen` (tool ke-11)

```shell
maria mgen counter.mv                     # generate counter.sv + counter.svh
maria mgen src/ -o build/gen              # semua .mv di src/ → build/gen
maria mgen types.mv counter.mv tb.mv      # banyak file sekaligus
maria mgen counter.mv --stdout            # print .sv ke stdout (debug)
maria mgen --check counter.mv             # exit 1 bila output tidak up-to-date (CI)
maria mgen --package mypkg                # bungkus definisi bersama dalam package
maria mgen --emit-svh-only / --emit-sv-only
maria mgen --verbose                      # report file yang dihasilkan
```

Integrasi pipeline:
- `maria run counter.mv` (F8 ✅): file `.mv` di-transpile **on-the-fly** ke
  buffer (svh + sv digabung, baris `` `include `` di-strip) lalu disuntikkan
  sebagai sumber inline ke pipeline normal — **tanpa menulis file apa pun**.
  Semua flag run (`.mv --top X -T 2000`, `--compile-only`, dst.) berfungsi
  seperti biasa; sumber `.mv` juga ikut MICD cache (hash konten buffer).
  Implementasi: `main.rs` — `read_source_bytes()` (buffer inline vs disk)
  + cabang preprocess khusus `.mv` di `run()`.
- **Multi-file (F9 ✅)**: `mgen` dan `run` men-transpile SEMUA file `.mv`
  sekaligus dengan **konteks gabungan** — tipe/package/konstanta dari satu
  file terlihat oleh file lain. `types.mv` mendefinisikan `Addr`, `counter.mv`
  memakainya via `use types_pkg::*`: keduanya lolos type-check & bisa
  disimulasikan bersama (`maria run types.mv counter.mv --top counter`).
  Implementasi: `crates/maria-mv/src/check.rs` `check_many()` (combined `Ctx`),
  `crates/maria-mv/src/lib.rs` `transpile_many()`, `crates/maria-tools/src/gen.rs` + `main.rs` batch.
- **`.mv` di SEMUA tool (F10 ✅)**: `sim`/`cov`/`elab`/`prof`/`bench`/`lint`/
  `check`/`inspect` menerima file `.mv` langsung — di-transpile ke buffer
  inline (svh+sv, tanpa menulis file) via `SessionConfig.inline_sources`,
  lalu pipeline CompileSession normal berjalan. Contoh:
  `maria sim tb.mv --top tb -T 2000`, `maria cov tb.mv`, `maria bench tb.mv`.
  Implementasi: `src/frontend/compile_session.rs` (`inline_sources`,
  preprocess dari buffer), `src/tools/mod.rs` `transpile_mv_to_inline()`
  dipakai `open_project`/`open_elaborated` (+ `prof.rs`/`bench.rs`).
  Catatan: tool berbasis buffer — path `.mv` **tidak direkam ke MICD**
  (hash basis buffer vs isi `.mv` di disk berbeda; setiap run di-transpile
  ulang), jalur mmap disk tetap zero-copy (tanpa `to_vec`), dan metrik
  `bench` (baris) dihitung dari buffer hasil transpile agar bermakna.
- Hasil generate di-cache MICD (`project/.maria/database/`) — `.mv` tidak
  berubah → tidak di-parse ulang.

### 11.1 Struktur implementasi (1 file = 1 tanggung jawab)

```
src/mv/mod.rs        — root, pipeline: lex → parse → check → codegen
src/mv/lexer.rs      — tokenizer .mv (gaya lexer SV Maria, reusable)
src/mv/parser.rs     — parser .mv → MvAst
src/mv/ast.rs        — tipe AST .mv (MvModule, MvPort, MvStmt, MvType, ...)
src/mv/check.rs      — type-check & semantic (diagnostics via src/diagnostics/)
src/mv/codegen.rs    — emitter SV (.sv + .svh), indah & deterministik
src/tools/gen.rs     — CLI `mgen` (mengikuti pola 10 tool existing)
```

`mgen` didaftarkan di `src/cli.rs` (`MariaCmd::Gen(MgenArgs)`) dan di-dispatch
di `main.rs` — mengikuti pola tool existing (`mfmt`, `melab`, dst.).

---

## 12. Contoh Lengkap

### 12.1 FSM — traffic light

**`traffic.mv`**

```mv
package traffic_pkg {
    enum State { RED, GREEN, YELLOW }
}

module traffic #(GREEN_T = 30, YELLOW_T = 5) {
    use traffic_pkg::*

    in  clk, rst_n : bit
    out state      : State
    out red, green, yellow : bit

    reg state : State = RED
    reg timer : uint[8] = 0

    seq(clk, rst_n) {
        if (!rst_n) {
            state <= RED
            timer <= 0
        } else {
            case (state) {
                RED: {
                    if (timer == GREEN_T) {
                        state <= GREEN
                        timer <= 0
                    } else {
                        timer <= timer + 1
                    }
                }
                GREEN: {
                    if (timer == YELLOW_T) {
                        state <= YELLOW
                        timer <= 0
                    } else {
                        timer <= timer + 1
                    }
                }
                YELLOW: {
                    state <= RED
                    timer <= 0
                }
            }
        }
    }

    comb {
        red    = (state == RED)
        green  = (state == GREEN)
        yellow = (state == YELLOW)
    }
}
```

**`traffic.svh`** (hasil generate)

```systemverilog
// ─────────────────────────────────────────────────────────────
// Generated by maria mgen v0.4.0 — DO NOT EDIT
// Sumber: traffic.mv
// Regenerate: maria mgen traffic.mv
// ─────────────────────────────────────────────────────────────
`ifndef TRAFFIC_SVH
`define TRAFFIC_SVH

package traffic_pkg;
    typedef enum logic [1:0] { RED, GREEN, YELLOW } State;
endpackage

`endif
```

**`traffic.sv`**

```systemverilog
// ─────────────────────────────────────────────────────────────
// Generated by maria mgen v0.4.0 — DO NOT EDIT
// Sumber: traffic.mv
// Regenerate: maria mgen traffic.mv
// ─────────────────────────────────────────────────────────────
`include "traffic.svh"

module traffic #(
    parameter int GREEN_T  = 30,
    parameter int YELLOW_T = 5
) (
    input  bit             clk,
    input  bit             rst_n,
    output traffic_pkg::State state,
    output bit             red,
    output bit             green,
    output bit             yellow
);
    traffic_pkg::State state_reg;
    logic [7:0] timer;

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            state_reg <= traffic_pkg::RED;
            timer     <= '0;
        end else begin
            case (state_reg)
                traffic_pkg::RED: begin
                    if (timer == GREEN_T) begin
                        state_reg <= traffic_pkg::GREEN;
                        timer     <= '0;
                    end else begin
                        timer <= timer + 1'b1;
                    end
                end
                traffic_pkg::GREEN: begin
                    if (timer == YELLOW_T) begin
                        state_reg <= traffic_pkg::YELLOW;
                        timer     <= '0;
                    end else begin
                        timer <= timer + 1'b1;
                    end
                end
                traffic_pkg::YELLOW: begin
                    state_reg <= traffic_pkg::RED;
                    timer     <= '0;
                end
                default: ;
            endcase
        end
    end

    assign state = state_reg;

    always_comb begin
        red    = (state_reg == traffic_pkg::RED);
        green  = (state_reg == traffic_pkg::GREEN);
        yellow = (state_reg == traffic_pkg::YELLOW);
    end
endmodule
```

> Catatan: `state` output di-drive via `assign` karena tipe enum — port enum
> tidak boleh di-assign dari always block langsung; codegen mendeteksi ini
> dan menurunkan `state_reg` + `assign` otomatis.

### 12.2 Testbench

**`tb_traffic.mv`**

```mv
module tb_traffic {
    in clk, rst_n : bit
    in state      : traffic_pkg::State
    in red, green, yellow : bit

    initial {
        clk = 0
        rst_n = 0
        #20
        rst_n = 1
        repeat (200) @(posedge clk)
        assert (yellow == 1) $info("yellow detected") else $fatal("no yellow")
        $finish
    }

    initial {
        forever #5 clk = ~clk
    }
}
```

---

## 13. Peta Implementasi (Roadmap)

| Fase | Cakupan | Deliverable | Verifikasi |
|------|---------|-------------|------------|
| **F1** ✅ | Lexer + parser `.mv` + AST | `src/mv/{mod,lexer,parser,ast}.rs` | unit test lex/parse |
| **F2** ✅ | Codegen dasar RTL | `src/mv/codegen.rs` + `mgen` CLI | `counter.mv` → SV valid, disimulasi Maria |
| **F3** ✅ | Tipe: alias/struct/enum/package + `.svh` | emitter `.svh` + `use pkg::*` | golden-file test output byte-identik |
| **F4** ✅ | `seq`/`comb`/reset + type-check | `src/mv/check.rs` (E2001–E2007) | 15 unit test check + contoh lolos |
| **F5** ✅ | Generate, instansiasi, function/task | for/if generate, named/positional inst | test hierarki 2-level |
| **F6** ✅ | Testbench: initial/final/assert/`$`-task + program block | `assert property` raw 1:1, `$`-task statement, `forever`/`repeat` compact | `tb_counter.mv` → generate → simulate pass |
| **F7** ✅ | Class & UVM subset | class/extends/field/rand field/constraint/func/task + method call | `class_model.mv` check penuh + compile; `my_test.mv` (UVM, `--no-check`) compile |
| **F8** ✅ | `maria run x.mv` — transpile on-the-fly | file `.mv` di-transpile ke buffer inline (svh+sv, tanpa menulis file) → pipeline normal + MICD | `maria examples/mv/tb_counter.mv --top tb_counter -T 2000` → simulasi pass (assert 0 gagal, coverage 100%) |
| **F9** ✅ | Multi-file `.mv` lintas-package | `check_many`/`transpile_many` (konteks gabungan) di `mgen` + `run` | `types.mv` + `counter.mv` (use `types_pkg::*`) → generate + sim bersama OK; solo check tetap E2005 |
| **F10** ✅ | `.mv` di SEMUA tool | `SessionConfig.inline_sources` (buffer transpile) → `open_project`/`open_elaborated` | `maria sim/cov/elab/prof/bench/lint/check/inspect <file>.mv` semua berjalan; regresi `.sv` utuh |
| **F11** ✅ | Error type-check berposisi `line:col` | span (line, col) di AST (`Expr::Ident/Sized/Scoped/Member`, `MvType::Named`, `Stmt::Assign`, semua deklarasi) → E2001–E2007 + snippet source & caret di `mgen`/`run` (`mv::format_error`) | 11 test posisi; `mgen`/`run` menampilkan `--> path:line:col` + baris + caret |
| **F12** ✅ | Constraint lanjutan: `inside`/`dist`/`solve`/`if`-else | sisi `.mv` (lexer/ast/parser/codegen/check) + sisi SV (`ConstraintItem::If`, solver `eval_constraint_body`, fix `:=` token, evaluasi `inside` range & lebar-bit) | 89 test `mv::`; `test_randomize_dist_if_solve_constraint` e2e solver; `tb_adv.mv` (inside+dist+if+solve) randomize → assert 0 gagal |
| **F13** ✅ | Fix precedence `? :` di parser SV (ternary = precedence PALING RENDAH, di bawah `||`) | guard `min_prec` pada `Token::Question` di `src/parser/expr.rs` | `test_ternary_precedence_lowest` + `test_ternary_in_assertion_with_class_field`; `m == 3 ? 1 : 0` kini `(m==3)?1:0` (bukan `m==1`); full suite 1523 pass; e2e `.mv` ternary → assert 0 gagal |
| **F14** ✅ | Severity system tasks `$info`/`$warning`/`$error`/`$fatal` | arm baru di `evaluate_lang_syscall` (block_syscall.rs, jalur IR x2) + `handle_ast_syscall` (eval/syscall.rs, jalur AST); format argumen = `format_display`; `$fatal` → `fatal_hit` (field baru engine) + `running=false` → blok statement berhenti SEKETIKA (final block tetap jalan); cek `fatal_hit` di 3 loop statement block.rs | `test_severity_tasks_info_error_fatal` (ok=1 sebelum $fatal, ok=2 tak tercapai) + `test_assert_severity_action_block`; e2e `.mv` `assert (c) $info else $fatal` → `Info:` tercetak, `Fatal:` stop; full suite 1525 pass |
| **F15** ✅ | Ringkasan severity akhir sim + exit code non-zero untuk `$fatal` | counter severity (4 field engine) di-increment tiap `$info`/`$warning`/`$error`/`$fatal`; `report_severity_summary()` di akhir `run()` core.rs (hanya dicetak bila > 0); `$fatal` → `Err(AssertionFailed)` → exit non-zero di `tools/sim.rs` + `main.rs` (run legacy & run_fast) — `$error`/`$warning` TIDAK mengubah exit code | e2e CLI: `$fatal` → exit 60, `$error` saja → exit 0; summary `── Severity Summary ──` tampil bila warning/error/fatal > 0; full suite 1526 pass |
| **F16** ✅ | UVM severity: `uvm_info`/`uvm_warning`/`uvm_error`/`uvm_fatal` + method `uvm_report_*` + fix preprocessor macro di awal baris | `uvm_macros.svh`: macro UVM → `$info`/`$warning`/`$error`/`$fatal`; `builtin.rs` `execute_uvm_report_object_method` → `emit_severity` (counter + `fatal_hit`); `eval/ast.rs`: `uvm_report_*` tanpa `this.` di body method class di-dispatch ke report builtin (sebelumnya "unknown function ... null default"); `preprocessor.rs`: baris backtick + nama = macro terdefinisi → invokasi (di-expand), bukan directive tak dikenal yang di-skip | `test_uvm_macro_severity_fatal` (`` `uvm_fatal `` hentikan blok, ok=2 tak tercapai) + `test_uvm_report_method_severity` (method path, ok=1 tak tercapai); e2e `uvm_macros.svh` include: Info/Warning/Error/Fatal tercetak, `uvm_fatal` → exit 60; full suite 1528 pass |
| **F17** ✅ | Macro sequence UVM end-to-end: `uvm_do`/`uvm_do_with`/`uvm_rand_send` + objection (`uvm_raise_objection`/`uvm_drop_objection` → end-of-test) | (1) `method.rs`: check builtin `randomize()` dipindah SEBELUM dispatch hierarki UVM (sebelumnya class UVM → "randomize not implemented"); (2) `constraint_solver.rs`: enum `InlineConstraint {Ast, Ir}` — `randomize() with {field}` di jalur AST (body method class) dievaluasi `evaluate_ast_expr` + `current_this` (field class ter-resolve, ikut domain analysis); (3) `eval/ast.rs`: `allocate_new_object`/`resolve_new_class_hint`/`eval_ast_assign_rhs` — `it = new()` di method class mengisi class_name dari tipe deklarasi local (sebelumnya "unknown class"); (4) `block.rs`: task body pakai helper sama; (5) `proc.rs` `parse_task`: decl tipe user-defined (`my_item it;`) masuk `decls` (sebelumnya task body decls kosong); (6) `util.rs` `format_display_ast` — `$sformatf` di jalur AST; (7) `parser/expr.rs`: `with` menembus unary `!` (pola UVM `!x.randomize() with {...}` = `!(x with {...})`) | `test_randomize_uvm_sequence_item` (constraint inside dihormati) + `test_randomize_with_inline_field_constraint` (`with {addr>200}` → got>200) + `test_uvm_do_macro_sequence_e2e` (uvm_do + objection, constraint addr==42); e2e `uvm_do` → `done addr=8`, `uvm_do_with` → `done addr=208`; full suite 1531 pass |
| **F18** ✅ | UVM phases + `run_test()` + komponen penuh (`uvm_env`/`uvm_agent` ditambahkan) + `uvm_test_top` global + `get_next_item` blocking | (1) `hierarchy.rs`: `run_uvm_test(name)` (guard `uvm_phases_started` — execute_phases auto-detect di run() TIDAK dobel), fase pre-run sinkron root→children (build/connect/eoe/sos), `run_phase` task async (delay → ContinueAstBlock), `execute_report_phases` (extract/check/report/final) dipanggil `drop_objection` SEBELUM `running=false`; children phase SELALU di-propagate walau root tak punya phase; (2) `run_test` statement (stmt.rs→SysCall) & ekspresi (expr.rs→SysFunc) — sebelumnya di-eliminasi diam-diam sbg side-effect-free; (3) `eval/ast.rs` Stmt::SysCall di `evaluate_ast_stmt` (function body) — sebelumnya `$display`/report di function di-skip; (4) `ContinueAstBlock` bawa konteks `(stmts, fork_id, this, method)` — resume task UVM tak kehilangan `current_this`; (5) `IrClassField.dtype` + `resolve_new_class_hint` resolve field class (`env = new(...)`) — parent component tertaut; (6) `parser/class.rs` arm `Token::Ident` untuk field bertipe user-defined (`my_env env;`); (7) `builtin.rs`: `uvm_env`/`uvm_agent` di BUILTIN classes; arm `set_sequencer` di sequence; `start` set field `m_sequencer` + `__sequencer`; (8) `uvm/util.rs`: port string/handle 1-bit TIDAK di-resize(1) (super.new(name,parent) kehilangan arg); (9) `block.rs`: `get_next_item` blocking (queue kosong → suspend + retry t+1, bukan return item null), `ast_loop_continuation` utk loop AST berdelay/block; (10) `expr.rs`: ident `uvm_test_top` → root_test_obj_id | `test_uvm_run_test_phases` (got=item via get_next_item, connect_phase child, report/final jalan via uvm_test_top); e2e `run_test` full: SEQ `addr=79` → DRV `got addr=79` → objection drop → REPORT/FINAL; full suite 1532 pass |
| **F20** ✅ | Semua warning/error runtime WAJIB mencantumkan `file:line:col` | (1) `parser/expr.rs`: 5 lokasi `Expr::Ident` yang sebelumnya `line:0,col:0` kini diisi posisi (dollar end-marker, system function tanpa paren, class_prefix, number-as-ident, `this`); (2) `engine/mod.rs`+`core.rs`: field `cur_src_line`/`cur_src_col` (`Cell<usize>`) + helper `set_cur_src_pos`/`cur_src_pos`/`cur_src_loc_str`; `emit_warning`/`emit_diag`/`diag_error`/`diag_fatal` fallback ke posisi saat ini; (3) `eval/ast.rs`: `evaluate_ast_expr` set posisi dari `Ident`/`FuncCall`/`ScopedIdent`; (4) AST `Stmt::SysCall` + `IrStmt::SysCall` + `IrExpr::SysFunc` + `IrStmt::Assert`/`Assume` kini membawa line/col (diisi parser/elaborator); `block.rs`/`block_syscall.rs`/`eval/expr.rs` panggil `set_cur_src_pos` sebelum emit; (5) `util.rs` `emit_severity`: output `Warning:`/`Error:`/`Fatal:` disuffix `(at file:line:col)`; (6) semua `eprintln` warning langsung (DPI, unknown system call, `$load_coverage_db`, unsupported sysfunc, VCD, assertion/assumption) → `emit_warning`/`diag_error_at`/`diag_warn_at` berposisi; (7) `elaborator/stmt.rs`: helper `expr_location` rekursif untuk posisi sub-ekspresi; (8) warning internal engine (`state.rs`/`signal_history.rs`) diberi label `(internal)` | `test_diag_runtime_has_source_location` (assertion gagal punya snippet `┌─ file:line:col`); e2e: severity `$warning`/`$error`/`$fatal` + RT0001/RT9003 + assertion failed semua menampilkan lokasi; full suite 1533 pass |
| **F19** ✅ | `uvm_config_db` wildcard path matching + `run_test` eksplisit menang atas auto-detect | (1) `uvm/config.rs` (baru): glob-matching `*`/`?` utk path `set`/`get` + `config_db_find` (exact match MENANG, lalu wildcard paling spesifik) + `uvm_object_full_path` (resolve `get(this,"")` ke path hierarki); (2) `hierarchy.rs`: `design_has_explicit_run_test()` — scan IR processes + AST method bodies utk `run_test(...)`; `core.rs run()`: `execute_phases()` (auto-detect) HANYA bila TIDAK ada run_test eksplisit — sebelumnya auto-detect di run() menang duluan, pilih class phase asal dari iterasi HashMap, lalu guard `uvm_phases_started` blokir `initial run_test("my_test")` → test build_phase (berisi set) tak pernah jalan; (3) `parser/proc.rs`: pola `pkg::func(...)` di body function/task TIDAK lagi dikira deklarasi `pkg::type name` (cek `peek_ahead(3)==LParen`) — sebelumnya `uvm_config_db::set(...)` membuat `parse_function` Err → method class hilang diam-diam; (4) `util.rs` `logicvec_to_string`: potong di byte NUL pertama (C-style) — NUL terminator dari `string_to_logicvec` bocor ke inst_path (`uvm_test_top\0.agent`) → wildcard tak pernah match | `test_uvm_config_db_wildcard_path` (`*.agent`, `uvm_test_top.*`, exact menang) + `test_uvm_config_db_run_test_hierarchy` (e2e: set di build_phase test → get(this,"") di agent → count=8); e2e `uvm_cfg_wild2` → `AGENT ok=1 count=8`; full suite 1535 pass |
| **F21** ✅ | `uvm_event` + `uvm_barrier` — sinkronisasi antar komponen UVM | (1) `types.rs`: `UvmEventData` (name/triggered/on) + `UvmBarrierData` (name/threshold/count) + `UvmSyncWaiter` (continuation AST + konteks this/method); (2) `engine/mod.rs`+`core.rs`: fields `uvm_event_data`/`uvm_barrier_data`/`uvm_sync_waiters`/`ast_fork_cont`; (3) `elaborator/mod.rs`: inject builtin `__uvm_event`/`__uvm_barrier` + mapping `extends uvm_event`/`uvm_barrier`; (4) `uvm/sync.rs` (baru, 1 file = 1 tanggung jawab): `execute_uvm_event_method` (new/trigger/triggered/is_on/reset/on_off) + `execute_uvm_barrier_method` (new/wait_for/wait_for_count/reset/get_threshold/set_threshold) + `uvm_try_wait` (blocking wait_*: register waiter + suspend) + `uvm_release_waiters` (trigger()/barrier penuh → ContinueAstBlock t+1); (5) `method.rs`: dispatch via `is_uvm_event_hierarchy`/`is_uvm_barrier_hierarchy` (telusur extends chain — objek dibuat dgn class_name asli `uvm_event` dari tipe field, bukan `__uvm_event`); (6) `eval/ast.rs`: `allocate_new_object`/`::new` dispatch `new` utk hierarchy event/barrier walau builtin `methods: vec![]` (find_method_in_hierarchy gagal) — tanpa ini data tak ter-insert → "uvm_event not initialized"; (7) `block.rs`: blocking `wait_trigger`/`wait_on`/`wait_for`/`wait_for_count` di `Stmt::Expr` (continuation = stmt SETELAH wait — `wait_for` count += 1 SEKALI); (8) AST `Stmt::Fork` rewrite: pakai `alloc_fork_group` + continuation AST di `ast_fork_cont` (ForkGroup.continuation cuma Vec<IrStmt>) + eksekusi di `fork_finish` — SEBELUMNYA remaining dijalankan LANGSUNG (join selalu dilewati → branch yg menunggu trigger/barrier ditinggalkan); (9) `event.rs` `ContinueAstBlock`: `fork_decrement` SEBELUM restore current_this (branch terakhir → fork_finish mengeksekusi cont AST yg masih butuh konteks method — sebelumnya restore duluan → field class gagal resolve, warning RT0001 + 0) | `test_uvm_event_sync` (trigger di branch #10, wait_trigger di branch lain, join, $error kanari) + `test_uvm_barrier_sync` (threshold 3, ketiga branch release bersamaan); e2e: `EVENT_OK` (t1=1 t2=1) + `BARRIER_OK` (a=1 b=1 c=1); full suite 1537 pass |
| **F22** ✅ | `uvm_subscriber` — komponen penerima broadcast `uvm_analysis_port` (TLM analysis) | (1) `elaborator/mod.rs`: `uvm_subscriber` masuk is_builtin + mapping `extends uvm_subscriber` → `__uvm_subscriber` + inject builtin `__uvm_subscriber` (extends `__uvm_component`, `methods: vec![]`); (2) `hierarchy.rs`: `is_uvm_subscriber_hierarchy` (telusur extends chain); (3) `builtin.rs` `execute_uvm_subscriber_method`: `new` → auto-buat analysis_imp child (class `__uvm_analysis_imp`, parent = subscriber, data `UvmAnalysisImpData` + `UvmObjectData`) + simpan id di field `analysis_imp` (pola UVM asli `uvm_subscriber` punya `analysis_imp` internal) — user cukup `mon.ap.connect(sub.analysis_imp)` lalu `ap.write(item)` → imp (`execute_uvm_analysis_imp_method`) → `parent.write` (override user); `write` tanpa override → no-op; fallback `execute_uvm_component_method`; (4) `builtin.rs` `execute_super_method`: arm subscriber SEBELUM component check (`super.new` di `my_sub extends uvm_subscriber` membangun imp); (5) `method.rs`: dispatch `!user_override && is_uvm_subscriber_hierarchy` (pola F21 — `write`/method custom override user tetap jalan normal); (6) FIX BUG F18 PRE-EXISTING: `execute_report_phases` — `call_phase_on_children` kini SELALU dipanggil walau root test TIDAK punya phase tsb (sebelumnya di dalam `if root punya phase` → env dgn check_phase/report_phase tak pernah dijalankan bila test tak punya — pola nyata monitor→subscriber) | `test_uvm_subscriber_analysis_broadcast` (monitor ap.write → sub.write override count/addr, $error kanari, check_phase child divalidasi); e2e `uvm_sub2` → `SUBSCRIBER_OK cnt=1 addr=42`; full suite 1539 pass |
| **F23** ✅ | `uvm_tlm_fifo` + `uvm_analysis_export` — buffer FIFO TLM blocking put/get/peek | (1) `types.rs`: `UvmTlmFifoData` (name/queue `VecDeque<ObjId>`/capacity/export_id) + `UvmSyncWaiter.wait_label` (label wait utk release selektif — field method tetap konteks resume); (2) `engine/mod.rs`+`core.rs`: fields `uvm_tlm_fifo_data`/`uvm_fifo_export_data`; (3) `elaborator`: is_builtin + mapping `extends uvm_tlm_fifo`/`uvm_analysis_export` + inject `__uvm_tlm_fifo` (extends component), `__uvm_analysis_export` (extends `__uvm_analysis_port` — passthrough: connect/write broadcast otomatis via port), `__uvm_fifo_export` (extends export); (4) `uvm/fifo.rs` (baru, 1 file = 1 tanggung jawab): `execute_uvm_tlm_fifo_method` (new/capacity/used/is_empty/is_full/flush + fallback non-blocking put/get/try_put/try_get/peek) — `new` auto-buat export analysis internal + field `analysis_export`; `execute_uvm_fifo_export_method` (`write` → put non-blocking ke fifo parent, penuh → warning); `uvm_try_fifo_wait` (blocking: get/peek saat kosong & put saat penuh → register waiter + suspend) + `uvm_fifo_release_waiters` (release selektif getter vs putter via wait_label); (5) `method.rs`/`eval/ast.rs`: dispatch + guard new-dispatch (pola F21/F22 — objek tanpa `new` override tetap dibuat datanya); (6) `block.rs` `Stmt::Expr`: blocking get/put/peek utk objek hierarchy fifo — statement get/put PREPEND ke continuation (saat resume dieksekusi ulang → pop + tulis lvalue utk get, push utk put; TANPA ini get yang suspend tak pernah pop → item diambil get berikutnya); `get`/`peek` menulis item ke lvalue arg via `write_ast_lvalue` | `test_uvm_tlm_fifo_blocking_put_get` (konsumen get 2x blocking + produsen put 2 item, r1=100 r2=200, used=0) + `test_uvm_tlm_fifo_analysis_export` (`fifo.analysis_export.write(a)` → get menerima a) + `test_uvm_tlm_fifo_blocking_put_full_and_peek` (blocking put saat penuh — putter suspend → getter pop → putter resume; peek blocking non-pop); e2e `FIFO_OK r1=100 r2=200` + `EXPORT_OK r1=77`; full suite 1542 pass; fix review: arm fifo + wait_* di block.rs digabung (obj dievaluasi sekali), dead-code arm "new" di `execute_uvm_fifo_export_method` dihapus (export dibuat langsung oleh fifo new) |
| **F24** ✅ | Handshake blocking `uvm_sequence`/`uvm_sequencer`/`uvm_driver` + `uvm_seq_item_port` — stimulus sequence-driven ala UVM asli | (1) `uvm/seq.rs` (baru, 1 file = 1 tanggung jawab): `execute_uvm_seq_item_port_method` (new/connect/get_next_item/item_done — delegasi ke sequencer ter-connect), `uvm_seqr_for` (resolve seqr utk driver/port/sequencer), `uvm_seq_pop` (pop + grant current), `uvm_seq_finish_blocks` (finish_item block hanya bila ada konsumen driver/port TERHUBUNG dan item masih di queue/current — pola F17 tanpa driver tidak deadlock), `uvm_seq_try_wait` + `uvm_seq_release_getters`/`uvm_seq_release_finishers` (waiter di `uvm_sync_waiters` keyed by sequencer, label `get_next_item`/`finish_item:{item}`, release selektif pola F23); (2) `types.rs` `UvmSeqItemPortData` + field engine `uvm_seq_item_port_data`; (3) `builtin.rs`: `start_item` → push + release getter; `item_done` → take current + release finisher (pop hanya bila item MASIH terdepan di queue — pola lama F17 tanpa pop; tanpa ini double-pop bila block.rs sudah pop); (4) `block.rs`: arm FuncCall `get_next_item`/`try_next_item` (upgrade jalur F18 polling-1ns → waiter-based: pop + GRANT + tulis lvalue arg via `write_ast_lvalue` — sebelumnya lvalue `it` TAK PERNAH ditulis → item tak sampai driver) + arm FuncCall `finish_item` (block sampai item_done) + arm MethodCall utk obj driver/port/sequencer; (5) FIX BUG: guard `new`-dispatch di jalur IR `eval/expr.rs` (module initial) — sebelumnya hanya `find_method_in_hierarchy("new")` → builtin UVM `methods: vec![]` → data driver/sequencer tak dibuat (`drv = new(...)` → "driver not initialized") — kini hierarchy UVM penuh (pola jalur AST); (6) FIX BUG: `is_uvm_driver_hierarchy`/`is_uvm_sequencer_hierarchy`/`is_uvm_sequence_hierarchy`/`is_uvm_seq_item_port_hierarchy` kenali nama user-level langsung (`uvm_sequencer` tanpa class def di design → `sqr = new(...)` gagal guard) — pola `is_uvm_event_hierarchy`; (7) hapus semua `[DBG-UVM]` sisa | `test_uvm_sequence_driver_blocking_handshake` (driver get_next_item blocking 3 item 100/200/300 + sequence start_item/finish_item blocking, $error kanari, `#50` delay setelah fork-join — IR fork module initial selesai premature bila task suspend, limitation pre-existing); e2e `HANDSHAKE5_OK got=3 last=300 sent=3`; full suite 1543 pass; regresi F17/F19-F23 (uvm_do, AGENT, EVENT, BARRIER, SUBSCRIBER, FIFO, EXPORT, REPORT/FINAL) semua hijau |
| **F25** ✅ | Integrasi tombol **Generate** + dukungan `.mv` di GUI (`src/gui/`) — transpile Maria HDL → SV langsung dari editor | (1) `app.rs` `trigger_generate(state)`: file `.mv` AKTIF (editor live content, tanpa save dulu) → `mv::transpile(content, base)` (type-check E2001–E2007 di level `.mv`) → tulis `.sv` + `.svh` di SAMPING file `.mv` → buka `.sv` hasil di editor + log baris per file; error → `❌ Generate: line:col: pesan` (`MvError.format()`); sinkron (transpile < ms, tanpa thread); (2) `toolbar.rs`: tombol `⚙ Generate` — `add_enabled` hanya saat file aktif ber-ekstensi `.mv`, tooltip Ctrl+G; (3) `app.rs` `handle_shortcuts`: `Ctrl+G` global (pola Ctrl+S/Ctrl+O — tidak membajak pengetikan editor); (4) `state.rs` `collect_mv_files()` — scan tree utk `.mv` (compile `collect_sv_files` TIDAK menyentuh `.mv` — file `.mv` di-transpile dulu, baru `.sv` di-compile) | build `--features gui` bersih; `maria-gui` jalan headless (xvfb) tanpa panic; full suite 1543 pass (tidak ada regresi) |
| **F26** ✅ | Perbaikan gap `.mv`: **interface lengkap** (§6.10), **case qualifiers** (`priority`/`unique`/`unique0` + `casez`/`casex`), **fix IR fork premature** (fork/join selesai saat task suspend) | (1) **Interface**: `ast.rs` `Modport` + `Interface` + `MvFile.interfaces`; `parser.rs` `parse_interface` (port `in/out` + `sig` → deklarasi signal interface; `modport` arah+nama, keyword-based tanpa `;`), `parse_modport`; `codegen.rs` `emit_interface` ke `.svh` (guard + interface + modport), `has_shared` termasuk interface, port module bertipe interface di-emit TANPA arah (`axi_lite axi_if`), `seq` clock bisa field interface (`seq(axi_if.clk)` — raw-slice postfix); `check.rs` validasi E2007 duplikat interface + E2001 modport→signal tak ada + E2005 tipe interface sah, port interface BEBAS di-drive (skip E2003); (2) **Case qualifiers**: lexer token `Priority`/`Unique`/`Unique0` + literal `3'b1??` wildcard `?` utk casez; `Stmt::Case` + field `qual`/`kind`; parser `parse_case_body`; codegen emisi `priority case`/`unique casez`; check ikut pola baru; (3) **IR fork premature**: arm loop AST (`LoopForever`/`LoopWhile`/`DoWhile`/`LoopFor`/`Repeat`) sebelumnya `break` internal saat body suspend → `Ok(true)` → fork decrement premature. Kini `return Ok(false)` — resume `ContinueAstBlock(fork_id)` (event.rs) yang decrement saat branch benar-benar selesai; `execute_method_body` meneruskan `active_fork_id` + set `task_suspended` (guard fork_branch_end); (4) contoh `examples/mv/axi_interface.mv`; **fix review**: `generate_with_ifaces(file, base, iface_names)` — konteks gabungan utk port interface lintas-file (`dut.mv` pakai `bus` dari `bus_def.mv` → tetap di-emit `bus b` tanpa arah, konsisten dgn `check_many`); `generate` (single-file) delegasi dgn interface file sendiri; guard `task_suspended` hanya saat `active_fork_id` terisi (tidak mengotori fork berikutnya); unit test interface-only `.svh` + cross-file | full suite **1552 pass** (+9: interface 7, fork 2); e2e `FORK_HANDSHAKE_OK got=3 last=300 sent=3` TANPA workaround `#50`; `HANDSHAKE5_OK` F24 tetap hijau; semua contoh `.mv` mgen OK; `mgen` direktori multi-file cross-interface OK (`bus b` tanpa arah) |
| **F27** ✅ | **Interface di simulator** — instance interface + port interface + clock hierarkis berjalan penuh di engine | (1) **Elaborator** (`mod.rs` port loop): port bertipe interface (`bus_if b` / `bus_if.m b`) → signal handle 64-bit + `iface_type` (strip modport utk pencocokan) — lebar port hanya utk type-check di flatten; (2) **Member access** (`expr.rs` rvalue + `stmt.rs` lvalue): field port interface (`b.clk`) dikompil sbg `HierRef("b.clk")` (bukan `VirtualIfaceAccess` — itu utk vif variabel yg di-bind runtime; diskriminator `iface_type.is_some() && class_name.is_none()`) → engine resolve via `hier_signal_map` (design sudah di-flatten) — baca & tulis field interface dari DUT & TB konsisten (signal flatten yg sama); (3) **Clock hierarkis**: `ir.rs` `ClockEdge::PosEdgeHier/NegEdgeHier(Symbol)`; `always.rs` `extract_clock_reset` fallback `posedge b.clk` (build_hier_name); `stmt.rs` EventControl `@(posedge b.clk)` prosedural + helper `hier_event_edge`; engine `clock_edge_signal` + `normalize_event_sigs` (arm-time resolve) — event.rs trigger Sequential + pending events, core.rs probe, block.rs 2 situs, lvalue.rs `find_ast_signal_id` MemberAccess → hier; flatten/sim_dag/cdc/clock_domain arms baru; (4) **Koneksi instance**: `instance_port_expr_to_signal` — ident yg menunjuk instance interface di module parent (`is_interface_instance`) → handle wire 64-bit (`__iface_<name>`) utk port_map flatten; (5) **Parser** (`instance.rs`): port `axi_lite.dut bus` (interface+modport) kini ter-parse → `dtype_name = "axi_lite.dut"` (sebelumnya rusak → E3001); (6) reset via field interface (`negedge bus.rst_n`) jalan (ResetInfo); **fix review**: (a) `clock_domain.rs` — key domain fusion resolve `PosEdgeHier` via `design.hier_signal_map` (hash fallback) — sebelumnya key tunggal `usize::MAX-1` salah-fuse antar clock interface berbeda saat cycle fusion; (b) nama handle koneksi unik `__iface_{hint}` (hint `inst.port`) — cegah duplikat nama signal utk dua koneksi ke instance interface yang sama; (c) fallback hier dibatasi: base harus port interface (iface_type tanpa class_name) ATAU instance interface di module yg sama — member access lain yang tak ter-resolve tetap error seperti lama (bukan process senyap) | full suite **1555 pass** (+3: `test_interface_port_dut_always_ff_hier_clock`, `test_interface_modport_port_procedural_event`, `test_interface_with_ports_connection`); e2e `IFACE_OK`, `MODPORT_OK rdata=2b`, `IFACE_PORTS_OK dout=23`, `ISO1_OK`; `HANDSHAKE5_OK` + `FORK_HANDSHAKE_OK` regresi UVM/fork tetap hijau; GUI build `--features gui` bersih |
| **F28** ✅ | **Interface end-to-end .mv → SV → sim** — hasil `mgen` untuk desain interface kini benar-benar bisa disimulasikan | (1) **Alias hier port interface di flatten** (`flatten.rs`): port interface child (`axi_if`) yang dikoneksikan ke instance interface parent (`bus`) — akses `axi_if.<field>` di child harus resolve ke signal flatten `bus.<field>`. Nama port ≠ nama instance (pola umum: `.mv` `sig bus : axi_lite` → `.axi_if(bus)`) — alias per field interface ditambahkan ke `hier_signal_map` saat flatten; (2) **Handle koneksi menyimpan nama instance** (`expr.rs`): `instance_port_expr_to_signal` set `class_name = Some(instance_name)` pada handle interface — nama signal handle berbasis hint (`__iface_inst_port`, fix review F27) tidak memuat nama instance, jadi flatten membaca instance path dari `class_name`; (3) **Codegen .mv** (`codegen.rs`): `sig x : iface` di module body di-emit sebagai **instance interface** (`axi_lite bus();`) — sebelumnya `axi_lite bus;` yang di-parse SV sbg **virtual interface var** (bukan instance) → `.axi_if(bus)` menunjuk vif variabel → akses field child `axi_if.awvalid` gagal. Kini `emit_module_kw` + `emit_module_item_at` (param `iface_names` baru, 5 pemanggil) mendeteksi tipe interface; (4) contoh `examples/mv/axi_interface.mv` di-upgrade jadi testbench hidup: clock generator via field interface (`bus.clk`), drive field (`bus.awvalid = 1`), `seq(axi_if.clk)` di slave, verifikasi `done`; **fix review**: alias hier diproses **POST-pass** — job `(port_name, iface_name, inst_name)` dikumpulkan saat flatten (`Elaborator.iface_alias_jobs`) lalu diproses di `flatten_instances` setelah SEMUA instance ter-flatten — tidak bergantung urutan AST (test `test_interface_alias_order_independent`: child module ditulis sebelum instance interface); `MvType::Array` tidak terpengaruh (interface array tetap emit lama); `class_name` handle hanya metadata (bukan class object) | full suite **1558 pass** (+3: `test_interface_port_alias_hier_diff_names` (sim, port `axi_if` vs instance `bus`), `codegen_sig_iface_emits_instance`); e2e: `maria mgen examples/mv/axi_interface.mv` → `maria sim ... --top tb_axi` → **`TB_AXI_OK done=1`** (tanpa error); regresi F27 semua hijau (`IFACE_OK`, `MODPORT_OK`, `IFACE_PORTS_OK`, `ALIAS_OK`, `ORDER_OK`, `ISO1_OK`); `HANDSHAKE5_OK` UVM + `IRFORK done=2` tetap hijau; GUI build bersih |
| **F30** ✅ | **Contoh .mv multi-file end-to-end + fix BUG literal hex (X di bit atas) + .sv tanpa konten fungsional tidak ditulis** | (1) **Contoh `examples/mv/multifile/`** (types.mv + dut.mv + tb.mv): `mgen` batch → `types.svh` (package `chip_types` + interface `mem_if`) + `dut.sv` + `tb.sv`; `sim types.svh dut.sv tb.sv --top tb_mem` → **`TB_MF_OK addr=6 st=1`** — bukti penuh rantai tipe lintas-file (F9), port interface (F26), alias hier port-interface (F28), validasi instansiasi LINTAS-FILE (F29): target module dari file lain dikenal di konteks gabungan → `.nope(st)` → `E2001 port 'nope' tidak ada di module 'memory_dut'` (test `f30_inst_cross_file_port_check`); (2) **BUG FIX — literal sized hex/octal/binary di-zero-extend** (`elaboration/util/const_fold.rs value_to_logicvec`): `LogicVec::new(w)` mengisi X, sehingga `16'h6` = 16'hxxxx...0006 → `signal == 16'h6` SELALU false (X di operand → hasil X). Kini fill Zero + digit x/z eksplisit tetap X/Z (sebelumnya `'x'` → `to_digit(16)`=None → 0). Ditemukan saat contoh F30 gagal: `addr == 16'h6` = 0 padahal addr=6 (probe `addr ^ 16'h6` = `xxxxxxxxxxxx0000`); (3) **`.sv` tanpa konten fungsional tidak ditulis** (`codegen.rs generate_sv` return kosong bila tanpa module/program/class/func/task; `gen.rs` skip tulis + `--check` menganggap kosong = up-to-date) — file package/interface-only (`types.mv`) tidak lagi menghasilkan `types.sv` yang isinya hanya header + `` `include `` (konsisten dgn skip .svh kosong) | full suite **1573 pass** (+8: `test_hex_literal_equality_fix`, `test_hex_literal_x_digit_stays_x` (sim), `value_to_logicvec_zero_extends_hex`, `value_to_logicvec_zero_extends_binary_and_octal`, `value_to_logicvec_binary_underscore_no_shift`, `value_to_logicvec_keeps_explicit_x`, `f30_inst_cross_file_port_check`, `f30_inst_cross_file_ok`); **fix review**: (1) arm `Binary` kini memfilter `_` dulu (konsisten dgn Hex/Octal) — underscore tidak lagi menggeser posisi digit (`8'b1_010` = 10), (2) `mgen` mendeteksi `.sv` basi dari generate versi lama (file definisi-only yang sudah tak ditulis) → warning `! obsolete types.sv terdeteksi — hapus manual`; e2e: `TB_MF_OK addr=6 st=1`, `mgen --check` stabil tanpa types.sv, regresi F27/F28 (`IFACE_OK`, `MODPORT_OK`, `IFACE_PORTS_OK`, `ALIAS_OK`, `TB_AXI_OK done=1`) + UVM `HANDSHAKE5_OK` hijau; GUI build bersih |
| **F29** ✅ | **Validasi instansiasi & koneksi port di check.rs** — kesalahan koneksi terdeteksi di level `.mv`, bukan di SV hasil generate (prinsip desain #4) | (1) **Ctx.modules + module_ports** (`check.rs`): indeks global kini berisi nama semua module/interface/program di file ini/gabungan + daftar port per target (value owned `Vec<String>`; key meminjam AST — tanpa `Box::leak`); (2) **Handler `MItem::Inst`**: BILA target dikenali (module/interface/program in-file) — nama port pada `.port(expr)` harus ada (E2001: "port 'x' tidak ada di module 'foo'"), jumlah koneksi positional tak boleh melebihi jumlah port (E2001: "terlalu banyak koneksi positional"). Nama eksternal (target di file `.mv` lain) **dilewati** — pola konservatif E2001/E2005 (multi-file tanpa false positive); (3) **Posisi error** (F11): `MItem::Inst` kini menyimpan line/col token nama module (`parse_inst`) sehingga error E2001 berposisi — `error[E1005]: file.mv: 8:10: [E2001] ...`; (4) **codegen.rs** update 2 pattern `MItem::Inst` (field baru line/col); (5) contoh `examples/mv/axi_interface.mv` sudah memakai named connection + drive field — validasi baru tidak memecahnya | full suite **1565 pass** (+7: `f29_inst_named_port_not_exist`, `f29_inst_positional_too_many`, `f29_inst_external_module_skipped`, `f29_inst_interface_port_check`, `f29_inst_ok_named_and_positional` + fix review `f29_review_duplicate_port_connection`, `f29_review_module_interface_name_collision`); **fix review**: (1) interface & module berbagi namespace tipe di SV — nama sama kini E2007 baik dalam-satu-file (`check_with_ctx`: cek silang `mod_names` di loop interface) maupun lintas-file (`check_many`: cek silang terhadap `mod_owner`), (2) port dikoneksikan dua kali (`.a(x), .a(y)`) → E2007 "dikoneksikan dua kali" (seen_ports di loop Conn::Named); e2e `mgen` negatif: `.bogus(y)` → `E2001` (line:col), `(x, y)` 2-koneksi-1-port → `E2001`, `.a(x), .a(y)` → `E2007`, interface+module `foo` → `E2007` (line:col), modul eksternal `foo3` → generate sukses; `maria mgen examples/mv/axi_interface.mv` → `maria sim ... --top tb_axi` → **`TB_AXI_OK done=1`**; GUI build bersih |
| **F31** ✅ | **Parameter module + override saat instansiasi** — `module counter_w #(WIDTH = 4, STEP = 1)` + `inst counter_w u_big #(.WIDTH(8), .STEP(2)) (...)` (nilai override benar di sim) + validasi nama param di check.rs (E2001/E2007) | (1) **BUG FIX parser SV** (`parser/instance.rs`): override param `#(...)` legal di DUA posisi SV — `mod #(.P(1)) u (...)` (Verilog-2001) DAN `mod u #(.P(1)) (...)` (umum). Sebelumnya hanya posisi pertama yang dikenali: `mod u #(...)` → blok param + port list TIDAK ter-parse → instance kosong (param_assigns=0, port_conns=0) → lebar port default + signal child tak pernah di-assign (bug siluman: instance override "jalan" tapi output stuck 0). Fix: helper `parse_param_block` (konsumsi `#(...)` → 2 map: nilai `Symbol→Expr` + tipe `Symbol→DataType`; item posisional `__paramN`) dipanggil di 2 posisi + `extend` (bukan timpa) agar kombinasi `mod #(.A(1)) u #(.B(2)) (...)` tetap jalan; (2) **BUG FIX engine — loop dgn body ber-delay/event hang** (`scheduler/block.rs` + `block_control.rs`): akar masalah `repeat (3) #1;` — elaborator meng-unroll repeat count-konstan menjadi `Block { stmts: [Delay, Delay, Delay] }`, lalu arm `IrStmt::Block` di `evaluate_block_with_delay_fork` TIDAK menyertakan statement SETELAH block (tail: `$display`/`$finish`) ke continuation saat inner suspend → tail hilang → sim hang tanpa output (probe `repeat`/`for`+delay tak pernah print). Fix sistemik: helper `with_tail_continuation` (set `loop_continuation` = tail + old) di 4 arm (Block/NamedBlock/If/Case) + 4 loop fork (`while`/`do-while`/`repeat`/`for`) kini combine `old_loop_cont` saat suspend — resume mengulang loop + lanjut tail (pola `while`/`do-while` yang sudah benar dipakai sbg referensi); (3) **Validasi check.rs**: `Ctx.module_params` (nama target → daftar param; collect_ctx module/program) + handler `MItem::Inst` — BILA target dikenal: override param tak dikenal → E2001 "parameter 'NOPE' tidak ada di module 'counter_w'" (line:col), override 2× → E2007 "di-override dua kali"; target eksternal (file .mv lain) dilewati — konservatif, sama dgn pola port F29; (4) contoh `examples/mv/param_counter.mv` (counter_w ber-param + tb dgn u_small default + u_big override) — body repeat eksplisit `{}` (hindari if tertelan) + `#1` setelah repeat; catatan SV race: `repeat (10) @(posedge clk)` lalu `$display` TANPA `#1` membaca nilai SEBELUM NBA settle (18/9 bukan 20/10) — perilaku engine benar, fix di tb | full suite **1589 pass** (+16: `f31_param_override_unknown`, `f31_param_override_duplicate`, `f31_param_override_ok`, `f31_param_override_external_module_skipped` + `test_if_branch_delay_keeps_tail`, `test_repeat_with_if_delay_keeps_loop_and_tail` (If/Case+delay tail) + regresi loop/parser); e2e `mgen param_counter.mv` → `sim --top tb_param` → **`TB_PARAM_OK cnt8=20 cnt4=10`** (10 posedge × STEP 2 = 20; default STEP 1 = 10); negatif: `#(.NOPE(4))` → `E2001`, `#(.W(4), .W(8))` → `E2007` (line:col); regresi loop: `RT1 repeat-delay OK`, `RT2 OK`, `RT3 n=5`, `F1 for-delay OK`, `W1 while-delay OK k=3`, `RPT2 cnt=20`, `BD tail-ok`; regresi F27/F28/F30 (`TB_AXI_OK done=1`, `TB_MF_OK addr=6 st=1`) + UVM `HANDSHAKE5_OK` hijau; GUI build bersih |
| **F32** ✅ | **Type parameter di .mv** — `module shifter #(T : type = logic[7:0], N = 2)` + `inst shifter u16 #(.T(Word16), .N(3)) (...)`; `T` sah sbg tipe port/signal & lebar-nya ter-resolve (default & override) | (1) **ast.rs**: `Param.type_default: Option<MvType>` — default TIPE utk type param; doc: bentuk (A) `T : type = logic[7:0]` dan (B) `type T = logic[15:0]` (kata kunci `type`); (2) **parser .mv**: `parse_base_type` arm `Tok::Type` → `MvType::Named("type")` (marker; sebelumnya "tipe tidak dikenal: Type"); `parse_param` — `type` di awal (B) ATAU marker di `:` (A) → default di-parse sbg TIPE (`parse_type`) ke `type_default`, bukan ekspresi; (3) **codegen.rs**: `parameter type T = logic [7:0]` (default = `emit_type(type_default)`; marker OR `type_default.is_some()` — bentuk B ty=None jatuh ke arm yang benar); (4) **check.rs**: `Scope.type_params` (nama → default tipe) + `check_type_scope` (Named `T` sah bila ada di scope.type_params — hindari E2005; dipakai di port/sig/reg/const/var-decl/func-task args) + `type_width` resolve type param ke lebar default (E2002 tetap aktif); `Ctx.module_type_params` (nama target → daftar type param; collect_ctx module+program) + handler `MItem::Inst` — override `.T(...)` divalidasi sbg TIPE: nama tipe dikenal (E2005 bila bukan tipe / tipe tak dikenal), target eksternal dgn nilai berbentuk tipe (`is_type_like`: typedef/enum/class/interface/`pkg::item`) dilewati konservatif; (5) **BUG FIX elaborator SV — type param tak pernah bekerja**: (a) `parser/decl.rs parse_param_list` SIMPAN range default type param (`T = logic [7:0]`) ke `ParamDecl.range` — sebelumnya di-parse lalu DIBUANG → `type_default = DataType::Logic` 1-bit → `T` selalu lebar 1 (probe: `T r; r = 8'h2A; q = r` → Q 0); `elaborator/mod.rs type_param_widths` hitung lebar dari `param.range` (const_eval_params, param-aware) fallback `type_default.width()`; (b) override type param `#(.T(Word16))` — `resolve_type_width(dt)` (resolve typedef) bukan `dt.width()` yang selalu 1 utk UserDefined → `type_param_map` lebar benar; (6) contoh `examples/mv/type_param.mv`; keterbatasan: override literal tipe `#(.T(logic[15:0]))` di SV murni belum didukung (DataType tanpa range) — pakai typedef (`#(.T(Word16))`) | full suite **1601 pass** (+12: `f32_type_param_decl_ok`, `f32_type_param_type_kw_syntax`, `f32_type_param_bad_default_type`, `f32_type_param_override_type_ok`, `f32_type_param_override_non_type_err`, `f32_type_param_override_unknown_type_err`, `f32_inst_external_type_param_like_skipped` + fix review `f32_review_const_of_type_param`, `f32_review_override_scoped_type`, `f32_review_type_kw_without_default` + `codegen_type_param`, `codegen_type_param_kw_syntax`); e2e `mgen type_param.mv` → `sim --top tb_tp` → **`TB_TP_OK q8=4 q16=8`** (default T=logic[7:0] N=2 → 01<<2=4; override T=Word16(16-bit) N=3 → 0001<<3=8); probe SV murni `T r; r=8'h2A; q=r` → `Q 42` (sebelum fix: Q 0); negatif: `#(.T(8'd4))` → `E2005 nilai override type param harus nama tipe` (line:col); **fix review**: (1) pass-1 `MItem::Const` di check_module kini `check_type_scope` (sebelumnya scope-less → `const c : T` false positive E2005), (2) override type param SCOPED `#(.T(pkg::Word16))` didukung (Expr::Scoped → `MvType::Named("pkg::Word16")`) — sebelumnya jatuh ke arm `other` → E2005 false positive, (3) bentuk (B) `type T` tanpa default: parser kini SELALU set marker `Named("type")` utk bentuk (B) → `sig x : T` dikenal (sebelumnya ty=None+type_default=None → is_tp false → E2005); regresi F31 (`TB_PARAM_OK cnt8=20 cnt4=10`), F30 (`TB_MF_OK addr=6 st=1`), F27 (`TB_AXI_OK done=1`) + UVM hijau; GUI build bersih |
| **F33** ✅ | **Type cast di .mv** — `Word16'(a)`, `logic'(a[0])`, `T'(a)` (type param), `pkg::T'(x)` (scoped) → emit SV cast + validasi lebar (E2005 tipe tak dikenal, E2002 truncation) | (1) **lexer .mv**: token `Tok::Quote` — `'` di-emit sbg Quote bila BUKAN literal Fill (`'0`) / Sized (`'b101`, `8'hFF`) — cast `T'(` kini ter-lex (sebelumnya "literal tidak valid setelah '"); (2) **ast .mv**: `Expr::Cast { ty: Box<MvType>, expr, line, col }` — Box<MvType> utk memutus rekursi Expr↔MvType (MvType::Logic menyimpan Expr tanpa Box → E0072); (3) **parser .mv** `parse_primary`: ident diikuti Quote → `parse_base_type` (tipe dasar/user-defined/scoped `pkg::T`) + `'(` + expr + `)`; (4) **codegen .mv** `emit_expr(Cast)` → `{emit_type(ty)}'({expr})` — emit apa adanya, SV valid; (5) **check.rs**: `check_expr(Cast)` validasi tipe target (E2005) + `expr_width(Cast)` = `type_width(ty)` (E2002 aktif: `q : logic[7:0] = Word16'(a)` → truncation 16→8 error); (6) **BUG FIX elaborator SV** — `resolve_cast_name_width` fallback baru: typedef level FILE (hasil `mgen` meng-emit typedef ke `.svh` BUKAN package) via `resolve_type_width(UserDefined)`/`typedef_map` — `Word16'(a)` sebelumnya None → resize 1 → data loss (w=0); kini 16 (SV murni probe `CR w=1234 lo=55`); keterbatasan: cast ke type param module (`T'(a)`) lebar 1 di engine (konteks type param tak sampai ke resolve_cast_name_width); (7) contoh `examples/mv/cast.mv` (cast_dut + tb_cast; sim butuh `cast.sv cast.svh`) | full suite **1605 pass** (+4: `f33_cast_ok`, `f33_cast_unknown_type_err`, `f33_cast_width_into_narrow_signal_err` (check) + `codegen_cast`); e2e `mgen cast.mv` → `sim cast.sv cast.svh --top tb_cast` → **`TB_CAST_OK w=1234 lo=55 b=0`** (Word16'(a)=16'h1234, logic'(a)=0→lo=55, bit'(a[0])=0); negatif: `q = NopeTy'(a)` → `E2005 tipe tak dikenal 'NopeTy'` (line:col); regresi F31 (`TB_PARAM_OK cnt8=20 cnt4=10`), F30 (`TB_MF_OK addr=6 st=1`), F27 (`TB_AXI_OK done=1`) + UVM hijau; GUI build bersih |
| **F34** ✅ | **Function local var ber-initializer + `return expr` bekerja penuh di inline** — `int n = x - 1;` di-emit ke temp signal (sebelumnya DIABAIKAN → temp X → hasil 0 siluman) + `return r` → assign result slot; **fix review**: initializer yang mengandung nested function call (`var y : int = dbl(v)`) ikut di-inline ulang via `inline_funcs_in_stmt` — konsisten dgn pemrosesan ulang body (sebelumnya hanya `rename_in_expr` → FuncCall tersisa → E2001) | (1) **BUG FIX `ast/inline.rs`** (dua jalur: function call `replace_func_calls_in_expr` & task `StmtAssign` LHS FuncCall): decl local ber-initializer (`int r = 0; int n = x - 1;`) TIDAK pernah di-emit — guard lama `rename_map.contains_key(&var.name)` selalu true (nama sudah di-rename di pass sebelumnya) sehingga init di-skip → `n` tetap X → loop `while (n > 0)` tak jalan → function return X/0 (probe .mv `clog2`: o=0 padahal SV murni `FCR o=8` benar). Perbaikan: kumpulkan init jobs (`local_init_jobs`/`task_init_jobs`) di pass pertama, emit `BlockingAssign temp = rename_in_expr(init)` SETELAH semua rename selesai; (2) **BUG FIX parser non-ANSI** (`parser/proc.rs parse_function` + `parse_task`): `function int clog2; input int x;` — keyword tipe `int` setelah `input` TIDAK dikonsumsi → port `x` masuk `func.decls` (bukan `func.ports`) → inline membuat temp GANDA (`__func_..._arg0` vs `x`) → argumen literal tak pernah ter-assign ke `x` (probe non-ANSI `NA o=0`, setelah fix `NA o=8`); (3) konversi `return expr` → assign result slot (sudah ada di inline `Stmt::Return(Some)` → `ret_name`) kini berfungsi penuh karena init benar; (4) contoh `examples/mv/func_local.mv` (`func clog2` + `func scale` dengan local init + return) | full suite **1612 pass** (+5: `test_func_local_decl_init_and_return` (clog2(256)=8 — loop while + init + return), `test_func_local_init_referencing_port` (scale(21)=42 — init `int w = v * 2` mereferensikan port), `test_func_local_init_referencing_other_local` (`int m = n + 1;` — chaining init antar-local, rename_map lengkap saat emit), `test_func_local_init_with_nested_call` (`quad(21)=42` via `int y = double(v)` — nested call di init ter-inline), `test_func_nonansi_multi_port_bare` (multi-port `input int a, b` + bare `input x` 1-bit → 31)); e2e `mgen func_local.mv` → `sim --top tb_fl` → **`TB_FL o=8 s=0`** (clog2(0x0100)=8; scale(0)=0 benar); SV murni: `ADD 7` (tanpa init tetap OK — regresi), `GX 9` (`int n = a-1; return n;` a=10, dulu 0), `FCR o=8` (clog2 lengkap dgn init+return, dulu 0); probe .mv nested init `TBQ o=42`; regresi F33 (`TB_CAST_OK w=1234 lo=55 b=0`), F31 (`TB_PARAM_OK cnt8=20 cnt4=10`), F30 (`TB_MF_OK addr=6 st=1`), F27 (`TB_AXI_OK done=1`) + UVM hijau; GUI build bersih |

**Kriteria selesai F2:** `cargo run -- mgen examples/counter.mv` menghasilkan
`counter.sv` yang bisa disimulasikan oleh `cargo run -- counter.sv` tanpa error.

---

## 14. Keputusan Desain yang Disepakati

1. **Bahasa baru total** — sintaks `.mv` sepenuhnya milik Maria; SV murni output.
2. **Cakupan semua** — RTL + testbench/verifikasi + class/UVM dalam satu bahasa.
3. **Hasil generate di-commit** — `.sv`/`.svh` ikut repo (CI/tool lain bisa pakai
   langsung); regenerasi deterministik membuat commit aman.
4. **Output indah & terbaca manusia** — emitter memprioritaskan keterbacaan.
5. **Type-check di level `.mv`** — error muncul di sumber, bukan hasil generate.
6. **Standar SV valid** — output kompatibel dengan tool EDA mana pun.

---

## 15. Referensi

- `DESIGN.md` — arsitektur compiler Maria (AST/HIR/MIR, cache, diagnostics)
- `AGENTS.md` — konvensi project (1 file = 1 tanggung jawab, larangan script)
- `src/tools/` — pola implementasi tool CLI (`mfmt`, `melab`, dst.)
- `src/cli.rs` / `src/main.rs` — pendaftaran & dispatch subcommand
- Chisel (Berkeley) & SpinalHDL — filosofi HCL yang diadopsi
